{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null && root.child('adminUsers').child(auth.uid).exists()",
    "presence": {
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
